name: agentic_worktrees
description: "Agentic Worktrees desktop control plane client."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  graphql: ^5.2.3
  shared_preferences: ^2.5.3
  logger: ^2.5.0
  path_provider: ^2.1.5
  gql: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.6.0
  graphql_codegen: ^3.0.1
  mockito: ^5.6.3

flutter:
  uses-material-design: true
