# Future prompts to line up for agent work

* When task board is json type. we must save that taskboard to postgres. worker must pull from postgres. we need to reimplement the local json filesystem taskboard now and move it fully into postgres for distrubuted sharing. We are also going to rename it from json to internal. ensure no part of the now named "internal" tracker is on filesystem. Remove all filesystem code related to the old json tracker.

* Change priority from string to int to make priority filtering easier higher the number the higher the priority. Convert string priorities from ingestion to int.

* Drop all implemenations of Tracker Ingestion implementations like our github issues implementations. or any others present in v1 or desktop client. so that were left with creating the project and board and not be able ingest anything to it yet. this will include dropping all env config for api and worker related to tracker boards it will now solely be configured by the user at project creation time.

* The scm provider is not something configured by api/worker at boot time it's during client project creation/management. which means securly storing the PAT we require & creating an scm provider repository to store and reference them in the project. this will currently be a per project scope of configuring scm provider & credentials. Ensure you touch all code in v1 worker/api & the frontend desktop. ensure you clean all code that does not comply with this leaving no track of how it's currently bootstrapped

* Vastly expand worker language support so agentic workflows can operate across many code languages (go, typescript/javascript, python, java, c#, rust, ruby, php, kotlin, swift, dart, c/c++, terraform, bash, sql, yaml/json/toml/markdown). Implement a language capability registry and per-language execution policy in worker, including detection, validation, formatting/lint/test command mapping, and safe toolchain invocation constraints. Ensure api contracts, worker domain/application/infrastructure layering, task payloads, persistence, telemetry, and frontend project settings are updated for language-aware behavior. Include migration/backward compatibility for existing projects, deterministic defaults when language is unknown, and full tests for policy enforcement and language-specific routing.

* Baseline worker tooling prompt for ingestion/system prompts: "Execution environment baseline (Docker worker): Debian-based container with shell access; core CLI: bash/sh, git, gh, copilot, curl, wget, tar, unzip, zip, make; data/query tooling: jq, yq, sqlite3; search/inspection tooling: rg, fd, tree, less, findutils, coreutils, moreutils, file, xxd; sync/automation tooling: rsync, fzf, entr; container tooling: docker + dockerd (DinD capable with privileged runtime); diagnostics tooling: procps (ps), lsof, nc, dig, ping, tcpdump, traceroute, mtr, strace, ltrace (amd64 builds); document/shell tooling: pandoc, shellcheck, shfmt, tini; language runtimes/toolchains: python3/pip3/venv, node/npm, java/javac, ruby, php, go/gofmt, rustc/cargo, gcc/g++, cmake, pkg-config; go developer tooling: gopls, goimports, dlv, staticcheck, golangci-lint, gotestsum, mockgen, govulncheck, task. Use only available tools, prefer deterministic non-interactive commands, and keep writes scoped to explicitly provided project paths."
